/** @deprecated Use `../design-system/tokens` */
export {
  getColors as getMobileColors,
  spacing,
  radius,
  space,
  layout,
  touch,
  legacyTypography,
} from '../design-system/tokens';

import {legacyTypography} from '../design-system/tokens';

/** Legacy shape: h1, h2, hero, … */
export const typography = legacyTypography;
