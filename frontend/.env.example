# Рекомендуется /api (прокси Vite). Прямой URL только если фронт и API на одном хосте.
VITE_API_URL=/api

# Яндекс.Диск OAuth (публичный client_id; секрет только в корневом .env проекта)
# VITE_YANDEX_CLIENT_ID=
